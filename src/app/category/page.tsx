import { HeaderOne } from '@/components/sections';

export default function BrowseCategories() {
  return (
    <main>
      <HeaderOne isHero={false} data={null} />
      <div className="h-screen w-full flex-center">
        <h2 className="my-5">BROWSE CATEGORIES PAGE</h2>
      </div>
    </main>
  );
}
